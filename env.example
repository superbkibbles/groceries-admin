# Environment variables for groceries admin panel
# Copy this file to .env.local and update the values as needed

# API Configuration
NEXT_PUBLIC_API_URL=http://91.99.95.75/api/v1

# Next.js Configuration
NODE_ENV=production
PORT=3000
HOSTNAME=0.0.0.0

# Optional: UploadThing Configuration (if using file uploads)
# UPLOADTHING_SECRET=your_uploadthing_secret
# UPLOADTHING_APP_ID=your_uploadthing_app_id

# Optional: Database Configuration (if using direct database access)
# DATABASE_URL=postgresql://username:password@localhost:5432/groceries_db

# Optional: Authentication Configuration
# NEXTAUTH_SECRET=your_nextauth_secret
# NEXTAUTH_URL=http://91.99.95.75/admin

# Optional: Redis Configuration (if using Redis for sessions/cache)
# REDIS_URL=redis://localhost:6379

# Optional: Email Configuration (if using email notifications)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your_email@gmail.com
# SMTP_PASS=your_app_password

# Optional: Analytics Configuration
# GOOGLE_ANALYTICS_ID=GA_MEASUREMENT_ID

# Optional: Sentry Configuration (for error tracking)
# SENTRY_DSN=your_sentry_dsn

# Optional: Logging Configuration
# LOG_LEVEL=info
# LOG_FORMAT=json
